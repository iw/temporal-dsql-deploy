[project]
name = "dsql-tests"
version = "0.1.0"
description = "Integration tests for Temporal with Aurora DSQL persistence"
requires-python = ">=3.14"
dependencies = [
    "temporalio>=1.22.0",
    "boto3>=1.42",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
